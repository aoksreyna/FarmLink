{
  "name": "farmlink-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix farmlink-frontend",
    "build": "npm run build --prefix farmlink-frontend",
    "start": "npm run start --prefix farmlink-frontend",
    "lint": "npm run lint --prefix farmlink-frontend"
  }
}
